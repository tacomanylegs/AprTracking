/**
 * 環境配置 - 直接在此設定 .env 路徑
 * 
 * 修改下面的 ENV_PATH 變數即可設定 .env 檔案位置
 * 優先順序：
 *   1. 本文件中的 ENV_PATH (最高優先)
 *   2. 命令行參數 --env-path
 *   3. 環境變數 ENV_PATH
 *   4. 預設位置 <project-root>/.env
 */

// ============ 🔧 在此設定你的 .env 路徑 ============

// 設定為 null 則使用自動搜尋
// 或直接輸入絕對路徑，例如：
//   const ENV_PATH = 'D:\\Game\\env';
//   const ENV_PATH = 'D:\\custom\\config\\.env';
//   const ENV_PATH = '/home/user/.env';

const ENV_PATH = "D:\\Game\\env"


// ============ 以下無需修改 ============

module.exports = {
  // 如果設定了路徑，將其設為環境變數
  // 這樣全部模組都會使用同一個路徑
  ...(ENV_PATH ? (() => {
    process.env.ENV_PATH = require('path').resolve(ENV_PATH);
    console.log(`📝 ENV_PATH 已設定: ${process.env.ENV_PATH}`);
    return {};
  })() : {})
};
